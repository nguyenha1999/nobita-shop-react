import React from 'react';

const Home = () => {
    return (
        <div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
            <div className="a" style={{ height: "100vh", background: "#fff" }}>laolaal
                const [propertyName] = arrayToDestruct
            </div>
        </div>
    )
}

export default Home
